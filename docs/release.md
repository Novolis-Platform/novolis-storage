# Release

Publish **Novolis.Storage.Abstractions**, **Novolis.Storage.Json**, and **Novolis.Storage.Sqlite** together after `dotnet build Novolis.Storage.slnx` succeeds.

Versions use `build/version.json`. Each package includes XML documentation and its README from `src/<PackageId>/README.md`.
